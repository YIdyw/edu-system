<!--component/navigator/navigator.wxml-->
<!-- <view class="navbar" style="height: {{navigationBarHeight}}px">
  <view style="height: {{statusBarHeight}}px"></view>
  <view class='title-container'>
    <view class='capsule' wx:if="{{ back || home }}">
      <view class="back" bindtap='back' wx:if="{{back}}">
        <image class="image" src='../../colorui/icon/back.png'></image>         
      </view>
      <view class="home" bindtap='backHome' wx:if="{{home}}">
        <image class="image" src='../../colorui/icon/home.png'></image>
      </view>
    </view>
    <view class='title'>{{text}}</view>
  </view>
</view>
<view style="height: {{navigationBarHeight}}px; background: #2655CF;"></view> -->

<!--component/navigator/navigator.wxml-->
<view class="navigator">

  <view class="navigator1"> </view>
  <view class="navigator2" bindtap="back">
    <text class="navigator3"> 乐学小优
		</text>

  </view>
</view>

<view  style="height:128rpx;"></view>
